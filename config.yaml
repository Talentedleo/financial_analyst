# LiteLLM Configuration (Optional - for advanced use cases)
# Note: Google ADK can call MiniMax directly via LiteLlm without LiteLLM proxy

model_list:
  - model_name: minimax/MiniMax-M2.1
    litellm_params:
      model: minimax/MiniMax-M2.1
      api_key: os.environ/MINIMAX_API_KEY
      api_base: https://api.minimax.io/v1

general_settings:
  master_key: sk-1234
