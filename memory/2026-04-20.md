# 2026-04-20 Daily Memory

## Project: financial_analyst

### Skills Rename (Hyphen Naming)
- Renamed all skill folders to hyphen naming (e.g., `warren_buffett` → `warren-buffett`)
- Updated SKILL.md `name` fields to hyphens
- ADK requires `load_skill_from_dir` with matching folder name
- Enable `FeatureName.SNAKE_CASE_SKILL_NAME` for underscore support, but name field still needs to match folder exactly

### Agent Factory Pattern
- Created `agents/experts/factory.py` with dynamic agent creation
- `create_expert_agent(celebrity_name)` - creates agent by skill folder name
- Falls back to `warren_buffett` if name not found
- Uses ADK `load_skill_from_dir` + `SkillToolset`

### API Routes
- Updated to use `get_available_celebrities()`, `list_all_expert_agents()`
- Default style: `warren_buffett`

### Git Commits
- Pushed PR #6 to main: dev branch with 11 expert agents

## Project: xyq-browser-skill

### Location
`~/Desktop/sandbox/openclaw_project/src/xyq-browser-skill`

### Key Findings
- SDK (`rc-client-security`) intercepts ALL HTTP requests with dynamic authentication
- Dynamic signature cannot be extracted - authentication is session-bound
- Direct API calls from outside browser fail due to SDK signature validation
- Correct approach: Browser automation (SDK handles auth automatically)
- Original trick `window.window.fetch` doesn't work (window.window === window)

### Browser Automation Workflow
1. `navigate_to_xyq()` - open xyq.jianying.com
2. `input_prompt()` - inject text into contenteditable
3. `click_generate()` - press Enter
4. `wait_for_result()` - poll for completion (1-5 min)
5. `get_generated_image_url()` - extract image URL

### Files Created
- `SKILL.md` - skill documentation
- `README.md` - usage instructions
- `agents/factory.py` - ADK agent factory
- `tools/browser_tools.py` - browser automation functions
- `skill.json` - OpenClaw skill config

### Git
- Pushed to dev branch

## General Notes
- Context 59% used, no compaction needed
- Service running on port 8000 for financial_analyst
